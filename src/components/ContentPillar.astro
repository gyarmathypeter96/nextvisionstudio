<section
  class="content-pillar"
  id="content-creation"
  aria-labelledby="content-pillar-title"
>
  <div class="content-pillar__inner">
    <div class="content-pillar__copy">
      <div class="content-pillar__label">Pillar 01 — Content Creation</div>

      <h2 id="content-pillar-title">
        Content That Makes Your Business Look Professional
      </h2>

      <p class="content-pillar__lead">
        Full-service social media content creation means you do not have to
        figure out what to post every week. We help Dublin businesses plan,
        shoot and create <strong>videos, photos and branded content</strong>{" "}
        that make them look active, modern and trustworthy online.
      </p>

      <ol class="content-pillar__points">
        <li>
          <span class="content-pillar__point-number" aria-hidden="true">01</span>
          <div>
            <strong>Clear ideas before we shoot</strong>
            <span>
              We plan content around your business, your services and what your
              audience needs to understand.
            </span>
          </div>
        </li>

        <li>
          <span class="content-pillar__point-number" aria-hidden="true">02</span>
          <div>
            <strong>Made for real social platforms</strong>
            <span>
              We create videos and photography for Reels, TikTok, YouTube
              Shorts, paid ads and your website.
            </span>
          </div>
        </li>

        <li>
          <span class="content-pillar__point-number" aria-hidden="true">03</span>
          <div>
            <strong>Easy for your business to use</strong>
            <span>
              You receive polished content assets that are ready to publish,
              reuse and build your brand around.
            </span>
          </div>
        </li>
      </ol>

      <div class="content-pillar__actions">
        <a href="#contact-form" class="content-pillar__primary">
          Get content ideas
        </a>
        <a href="#services" class="content-pillar__secondary">
          Videography Dublin
        </a>
      </div>
    </div>

    <aside class="video-card" aria-label="NextVision Studio Instagram platform guide">
      <div class="video-card__top">
        <span>Platform guide</span>
        <div class="video-card__badge">
          <i aria-hidden="true"></i>
          Instagram Reel
        </div>
      </div>

      <div class="video-card__frame">
        <button
          class="video-card__facade"
          type="button"
          data-instagram-embed="https://www.instagram.com/p/Da2M-s8D_TN/embed/"
          data-video-title="Which social media platform is best for your business — NextVision Studio Instagram Reel"
          aria-label="Play the Instagram Reel on this page: Which social media platform is best for your business"
        >
          <img
            src="/images/content/social-platform-guide.webp"
            alt="NextVision Studio guide to choosing a social media platform for your business"
            width="1280"
            height="720"
            loading="lazy"
            decoding="async"
          />
          <span class="video-card__play" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="currentColor">
              <path d="M9 7.6v8.8L16.2 12 9 7.6z"></path>
            </svg>
          </span>
        </button>
      </div>

      <p class="video-card__caption">
        <strong>In this video:</strong> which social media platform is best for
        awareness, trust, reach and enquiries.
        <a
          href="https://www.instagram.com/p/Da2M-s8D_TN/"
          target="_blank"
          rel="noopener noreferrer"
          aria-label="Open this Reel on Instagram in a new tab"
        >
          View on Instagram <span aria-hidden="true">↗</span>
        </a>
      </p>
    </aside>
  </div>
</section>

<script>
  const setupInstagramFacades = () => {
    document.querySelectorAll<HTMLButtonElement>("[data-instagram-embed]").forEach(
      (facade) => {
        if (facade.dataset.ready === "true") return;
        facade.dataset.ready = "true";

        facade.addEventListener(
          "click",
          () => {
            const embedUrl = facade.dataset.instagramEmbed;
            if (!embedUrl) return;

            const iframe = document.createElement("iframe");
            iframe.className = "video-card__iframe";
            iframe.src = embedUrl;
            iframe.title =
              facade.dataset.videoTitle ?? "NextVision Studio Instagram Reel";
            iframe.allow =
              "autoplay; encrypted-media; picture-in-picture; fullscreen";
            iframe.referrerPolicy = "strict-origin-when-cross-origin";
            iframe.allowFullscreen = true;

            facade.replaceWith(iframe);
          },
          { once: true },
        );
      },
    );
  };

  setupInstagramFacades();
  document.addEventListener("astro:page-load", setupInstagramFacades);
</script>

<style>
  .content-pillar,
  .content-pillar * {
    box-sizing: border-box;
  }

  .content-pillar {
    width: 100%;
    margin: 0;
    padding: 110px 24px 118px;
    overflow: hidden;

    border-top: 1px solid rgba(255, 255, 255, 0.055);
    border-bottom: 1px solid rgba(255, 255, 255, 0.055);
    background: #111111;
    color: #ffffff;
  }

  .content-pillar__inner {
    display: grid;
    grid-template-columns: 1.04fr 0.96fr;
    align-items: center;
    gap: 72px;

    width: min(100%, 1180px);
    margin: 0 auto;
  }

  .content-pillar__copy {
    max-width: 610px;
  }

  .content-pillar__label {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 22px;
    padding: 10px 17px;

    border: 1px solid rgba(180, 220, 255, 0.28);
    border-radius: 999px;
    background: rgba(155, 210, 255, 0.08);
    color: #cfeaff;

    font-size: 12px;
    font-weight: 900;
    line-height: 1;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .content-pillar h2 {
    margin: 0;
    color: #ffffff;
    font-size: clamp(42px, 5.2vw, 70px);
    font-weight: 900;
    line-height: 0.96;
    letter-spacing: -0.065em;
    text-wrap: balance;
  }

  .content-pillar__lead {
    margin: 28px 0 0;
    color: rgba(255, 255, 255, 0.7);
    font-size: clamp(17px, 1.7vw, 20px);
    font-weight: 500;
    line-height: 1.6;
  }

  .content-pillar__lead strong {
    color: #ffffff;
    font-weight: 800;
  }

  .content-pillar__points {
    display: grid;
    gap: 14px;
    margin: 34px 0 0;
    padding: 0;
    list-style: none;
  }

  .content-pillar__points li {
    display: grid;
    grid-template-columns: 34px 1fr;
    align-items: start;
    gap: 14px;
    padding: 17px 18px;

    border: 1px solid rgba(180, 220, 255, 0.12);
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.043);
    box-shadow: 0 18px 46px rgba(0, 0, 0, 0.16);

    transition:
      transform 260ms ease,
      border-color 260ms ease,
      background 260ms ease;
  }

  .content-pillar__points li:hover {
    border-color: rgba(180, 220, 255, 0.24);
    background: rgba(255, 255, 255, 0.058);
    transform: translateX(5px);
  }

  .content-pillar__point-number {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 34px;
    height: 34px;

    border: 1px solid rgba(180, 220, 255, 0.18);
    border-radius: 12px;
    background: rgba(155, 210, 255, 0.11);
    color: #d9eeff;
    font-size: 13px;
    font-weight: 900;
  }

  .content-pillar__points strong {
    display: block;
    margin-bottom: 5px;
    color: #ffffff;
    font-size: 15px;
    font-weight: 800;
    line-height: 1.25;
  }

  .content-pillar__points li div > span {
    display: block;
    color: rgba(255, 255, 255, 0.62);
    font-size: 14px;
    font-weight: 480;
    line-height: 1.45;
  }

  .content-pillar__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    margin-top: 36px;
  }

  .content-pillar__actions a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 52px;
    padding: 0 24px;

    border-radius: 999px;
    font-size: 13px;
    font-weight: 900;
    letter-spacing: 0.07em;
    text-decoration: none;
    text-transform: uppercase;

    transition:
      transform 250ms ease,
      background 250ms ease,
      border-color 250ms ease;
  }

  .content-pillar__primary {
    border: 1px solid rgba(180, 220, 255, 0.34);
    background: rgba(155, 210, 255, 0.14);
    color: #ffffff;
    box-shadow:
      inset 0 1px 1px rgba(255, 255, 255, 0.16),
      0 0 32px rgba(155, 210, 255, 0.12),
      0 18px 42px rgba(0, 0, 0, 0.26);
  }

  .content-pillar__secondary {
    border: 1px solid rgba(255, 255, 255, 0.12);
    background: rgba(255, 255, 255, 0.035);
    color: rgba(255, 255, 255, 0.78);
  }

  .content-pillar__actions a:hover,
  .content-pillar__actions a:focus-visible {
    border-color: rgba(180, 220, 255, 0.42);
    background: rgba(155, 210, 255, 0.18);
    color: #ffffff;
    transform: translateY(-3px);
  }

  .content-pillar__actions a:focus-visible {
    outline: 2px solid #cfeaff;
    outline-offset: 4px;
  }

  .video-card {
    width: min(100%, 420px);
    margin: 0 auto;
    padding: 22px;
    overflow: hidden;

    border: 1px solid rgba(180, 220, 255, 0.18);
    border-radius: 32px;
    background: linear-gradient(
      145deg,
      rgba(28, 36, 43, 0.96),
      rgba(16, 20, 24, 0.94)
    );
    box-shadow:
      0 28px 80px rgba(0, 0, 0, 0.36),
      inset 0 1px 1px rgba(255, 255, 255, 0.1);
  }

  .video-card__top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    margin-bottom: 16px;
  }

  .video-card__top > span {
    color: #cfeaff;
    font-size: 11px;
    font-weight: 900;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .video-card__badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 11px;

    border: 1px solid rgba(180, 220, 255, 0.18);
    border-radius: 999px;
    background: rgba(155, 210, 255, 0.1);
    color: rgba(255, 255, 255, 0.78);
    font-size: 11px;
    font-weight: 800;
    white-space: nowrap;
  }

  .video-card__badge i {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #9bd2ff;
    box-shadow: 0 0 18px rgba(155, 210, 255, 0.75);
  }

  .video-card__frame {
    position: relative;
    width: 100%;
    aspect-ratio: 9 / 16;
    overflow: hidden;

    border: 1px solid rgba(180, 220, 255, 0.16);
    border-radius: 24px;
    background: #05070a;
    box-shadow: 0 22px 58px rgba(0, 0, 0, 0.38);
  }

  .video-card__facade,
  .video-card__frame :global(.video-card__iframe) {
    position: absolute;
    inset: 0;
    display: block;
    width: 100%;
    height: 100%;
    border: 0;
  }

  .video-card__facade {
    margin: 0;
    padding: 0;
    overflow: hidden;
    background: #05070a;
    cursor: pointer;
  }

  .video-card__facade img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    transition:
      transform 400ms cubic-bezier(0.22, 1, 0.36, 1),
      filter 400ms ease;
  }

  .video-card__facade::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.28), transparent 48%);
    pointer-events: none;
  }

  .video-card__play {
    position: absolute;
    top: 50%;
    left: 50%;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 72px;
    height: 72px;

    border: 1px solid rgba(255, 255, 255, 0.5);
    border-radius: 50%;
    background: rgba(14, 21, 27, 0.66);
    color: #ffffff;
    box-shadow:
      0 0 38px rgba(155, 210, 255, 0.24),
      inset 0 1px 1px rgba(255, 255, 255, 0.28);
    backdrop-filter: blur(12px);

    transform: translate(-50%, -50%) scale(1);
    transition:
      transform 300ms cubic-bezier(0.22, 1, 0.36, 1),
      background 300ms ease;
  }

  .video-card__play svg {
    width: 36px;
    height: 36px;
  }

  .video-card__facade:hover img {
    filter: brightness(1.08);
    transform: scale(1.035);
  }

  .video-card__facade:hover .video-card__play,
  .video-card__facade:focus-visible .video-card__play {
    background: rgba(42, 72, 94, 0.74);
    transform: translate(-50%, -50%) scale(1.08);
  }

  .video-card__facade:focus-visible {
    outline: 2px solid #cfeaff;
    outline-offset: -4px;
  }

  .video-card__caption {
    margin: 17px 2px 0;
    color: rgba(255, 255, 255, 0.66);
    font-size: 14px;
    font-weight: 500;
    line-height: 1.48;
    text-align: center;
  }

  .video-card__caption strong {
    color: #ffffff;
    font-weight: 800;
  }

  .video-card__caption a {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    margin-left: 5px;
    color: #cfeaff;
    font-weight: 800;
    text-decoration: none;
    white-space: nowrap;
  }

  .video-card__caption a:hover {
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .video-card__caption a:focus-visible {
    border-radius: 3px;
    outline: 2px solid #cfeaff;
    outline-offset: 3px;
  }

  @media (max-width: 980px) {
    .content-pillar__inner {
      grid-template-columns: 1fr;
      gap: 56px;
    }

    .content-pillar__copy {
      max-width: 820px;
      margin: 0 auto;
      text-align: center;
    }

    .content-pillar__points {
      width: min(100%, 720px);
      margin-right: auto;
      margin-left: auto;
      text-align: left;
    }

    .content-pillar__actions {
      justify-content: center;
    }

    .video-card {
      max-width: 390px;
    }
  }

  @media (max-width: 720px) {
    .content-pillar {
      padding: 88px 16px 92px;
    }

    .content-pillar h2 {
      font-size: clamp(38px, 11vw, 54px);
      line-height: 0.98;
    }

    .content-pillar__lead {
      font-size: 16px;
    }

    .content-pillar__points li {
      grid-template-columns: 1fr;
      gap: 12px;
    }

    .content-pillar__actions {
      flex-direction: column;
    }

    .content-pillar__actions a {
      width: 100%;
    }

    .video-card {
      width: min(100%, 340px);
      padding: 16px;
      border-radius: 26px;
    }

    .video-card__top {
      align-items: flex-start;
    }

    .video-card__frame {
      border-radius: 20px;
    }

    .video-card__caption {
      font-size: 13px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .content-pillar__points li,
    .content-pillar__actions a,
    .video-card__facade img,
    .video-card__play {
      transition: none;
    }
  }
</style>
