<section class="services" id="services" aria-labelledby="services-title">
  <div class="services__inner">
    <header class="services__header">
      <div class="services__kicker">Our services</div>

      <h2 id="services-title">How we help Dublin businesses</h2>
    </header>

    <div class="services__grid">
      <a class="service-card service-card--video" href="/videography-dublin/" data-cta-placement="service-video">
        <div class="service-card__copy">
          <h3>Video production</h3>
          <p>
            Brand films, event coverage and short-form video that make your value clear.
          </p>
        </div>
      </a>

      <a class="service-card service-card--ads" href="/google-ads-meta-ads-dublin/" data-cta-placement="service-ads">
        <div class="service-card__copy">
          <h3>Google Ads &amp; Meta Ads</h3>
          <p>
            Campaigns, creative and landing pages managed around one useful number: qualified enquiries.
          </p>
        </div>
      </a>

      <a class="service-card service-card--social" href="/social-media-content-creation-dublin/" data-cta-placement="service-social">
        <div class="service-card__copy">
          <h3>Social content</h3>
          <p>
            Batch-planned Reels, Shorts and imagery that keep your business visible.
          </p>
        </div>
      </a>

      <a class="service-card service-card--web" href="/webdesigner-dublin/" data-cta-placement="service-web">
        <div class="service-card__copy">
          <h3>Website design &amp; development</h3>
          <p>
            Fast websites that explain your offer, build trust and lead visitors to enquire.
          </p>
        </div>
      </a>

      <a class="service-card service-card--crm" href="/crm-lead-automation-dublin/" data-cta-placement="service-crm">
        <div class="service-card__copy">
          <h3>CRM &amp; lead automation</h3>
          <p>
            A clear pipeline and practical follow-up workflows that help your team keep enquiries moving.
          </p>
        </div>
      </a>
    </div>
  </div>
</section>

<style>
  .services,
  .services * {
    box-sizing: border-box;
  }

  .services {
    position: relative;
    width: 100%;
    margin: 0;
    padding: 96px 24px 100px;
    overflow: hidden;

    background: #111111;
    color: #ffffff;
  }

  .services__inner {
    position: relative;
    width: min(100%, 1180px);
    margin: 0 auto;
  }

  .services__header {
    width: min(100%, 900px);
    margin: 0 auto 44px;
    text-align: center;
  }

  .services__kicker {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 22px;
    padding: 10px 18px;

    border: 1px solid rgba(180, 220, 255, 0.28);
    border-radius: 999px;
    background: rgba(155, 210, 255, 0.08);
    color: #cfeaff;

    font-size: 12px;
    font-weight: 900;
    line-height: 1;
    letter-spacing: 0.13em;
    text-transform: uppercase;

    box-shadow:
      inset 0 1px 1px rgba(255, 255, 255, 0.18),
      0 0 24px rgba(155, 210, 255, 0.08);
  }

  .services__header h2 {
    margin: 0;
    color: #ffffff;
    font-size: clamp(40px, 5vw, 60px);
    font-weight: 900;
    line-height: 0.95;
    letter-spacing: 0;
    text-wrap: balance;
  }

  .services__header p {
    width: min(100%, 790px);
    margin: 26px auto 0;
    color: rgba(255, 255, 255, 0.68);

    font-size: clamp(17px, 2vw, 20px);
    font-weight: 550;
    line-height: 1.5;
    text-wrap: balance;
  }

  .services__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 250px), 1fr));
    gap: 26px;
  }

  .service-card {
    --card-accent: 155, 210, 255;
    --card-image: url("/images/home-services/video-production-bg.webp");

    position: relative;
    isolation: isolate;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    min-width: 0;
    min-height: 400px;
    padding: 34px;
    overflow: hidden;

    border: 1px solid rgba(var(--card-accent), 0.3);
    border-radius: 26px;
    outline: none;
    background: #111;

    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.045),
      0 28px 70px rgba(0, 0, 0, 0.26);

    transform: translateY(0) scale(1);
    transition:
      transform 360ms cubic-bezier(0.22, 1, 0.36, 1),
      border-color 360ms ease,
      box-shadow 360ms ease;
    -webkit-tap-highlight-color: transparent;
    color: inherit;
    text-decoration: none;
  }

  .service-card::before,
  .service-card::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .service-card::before {
    z-index: -2;
    background-image: var(--card-image);
    background-position: center;
    background-size: cover;
    filter: grayscale(1) blur(3px) brightness(0.84);
    opacity: 0.6;
    transform: scale(1.08);
    transition:
      filter 360ms ease,
      opacity 360ms ease,
      transform 360ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .service-card::after {
    z-index: -1;
    background: linear-gradient(
      135deg,
      rgba(7, 10, 14, 0.48),
      rgba(5, 7, 10, 0.86)
    );
    opacity: 1;
    transition: opacity 360ms ease;
  }

  .service-card--video {
    --card-image: url("/images/home-services/video-production-bg.webp");
  }

  .service-card--ads {
    --card-image: url("/images/home-services/google-meta-ads-bg.webp");
    --card-accent: 130, 180, 220;
  }

  .service-card--social {
    --card-image: url("/images/home-services/social-content-bg.webp");
    --card-accent: 130, 180, 220;
  }

  .service-card--web {
    --card-image: url("/images/home-services/website-design-bg.webp");
    --card-accent: 190, 225, 255;
  }

  .service-card--crm {
    --card-image: url("/images/home-services/crm-lead-bg.webp");
    --card-accent: 130, 180, 220;
  }

  .service-card:hover,
  .service-card:focus-visible {
    border-color: rgba(var(--card-accent), 0.7);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.09),
      0 0 42px rgba(var(--card-accent), 0.1),
      0 36px 86px rgba(0, 0, 0, 0.38);
    transform: translateY(-10px) scale(1.012);
  }

  .service-card:hover::before,
  .service-card:focus-visible::before {
    filter: grayscale(0.92) blur(1.5px) brightness(0.9);
    opacity: 0.68;
    transform: scale(1.035);
  }

  .service-card:hover::after,
  .service-card:focus-visible::after {
    opacity: 0.82;
  }

  .service-card:focus-visible {
    outline: 2px solid #cfeaff;
    outline-offset: 5px;
  }

  .service-card__copy {
    position: relative;
    z-index: 1;
  }

  .service-card h3 {
    margin: 0 0 18px;
    color: #ffffff;
    font-size: clamp(27px, 2.45vw, 34px);
    font-weight: 850;
    line-height: 0.98;
    letter-spacing: -0.055em;
    text-wrap: balance;
  }

  .service-card p {
    margin: 0;
    color: rgba(255, 255, 255, 0.68);
    font-size: 15.5px;
    font-weight: 500;
    line-height: 1.62;
  }

  @media (max-width: 980px) {
    .services__grid {
      grid-template-columns: 1fr;
      width: min(100%, 680px);
      margin: 0 auto;
    }

    .service-card {
      min-height: 340px;
    }

  }

  @media (max-width: 640px) {
    .services {
      padding: 92px 16px 82px;
    }

    .services__header {
      margin-bottom: 46px;
    }

    .services__header h2 {
      font-size: clamp(39px, 12vw, 54px);
    }

    .services__header p {
      font-size: 16px;
      line-height: 1.55;
    }

    .service-card {
      min-height: 250px;
      padding: 28px;
      border-radius: 24px;
    }

    .service-card h3 {
      font-size: clamp(27px, 8.2vw, 32px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .service-card,
    .service-card::before,
    .service-card::after {
      transition: none;
    }
  }
</style>
