<section class="services" id="services" aria-labelledby="services-title">
  <div class="services__inner">
    <header class="services__header">
      <div class="services__kicker">Our services</div>

      <h2 id="services-title">Build Your Digital Presence</h2>

      <p>
        NextVision Studio helps Dublin businesses grow through professional
        videography, photography, social media management and conversion-focused
        website design.
      </p>
    </header>

    <div class="services__grid">
      <article class="service-card service-card--content" tabindex="0">
        <div class="service-card__icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <rect x="3" y="5" width="18" height="14" rx="2"></rect>
            <path d="M8 5l2 4"></path>
            <path d="M13 5l2 4"></path>
            <path d="M18 5l2 4"></path>
            <path d="M3 9h18"></path>
            <path d="M10 13l5 3-5 3v-6z"></path>
          </svg>
        </div>

        <div class="service-card__copy">
          <h3>Videography, Photography &amp; Content Creation</h3>
          <p>
            Professional videography in Dublin, short-form video production,
            brand photography, Reels, TikToks, Shorts and visual content created
            to make your business impossible to ignore online.
          </p>
        </div>
      </article>

      <article class="service-card service-card--social" tabindex="0">
        <div class="service-card__icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path d="M4 13v-2a2 2 0 0 1 2-2h2l8-4v14l-8-4H6a2 2 0 0 1-2-2z"></path>
            <path d="M18 9.5a4 4 0 0 1 0 5"></path>
            <path d="M20.5 7a8 8 0 0 1 0 10"></path>
            <path d="M8 15l1.5 4"></path>
          </svg>
        </div>

        <div class="service-card__copy">
          <h3>Social Media Management &amp; Paid Ads</h3>
          <p>
            Strategic content calendars, captions, publishing support and social
            media management, supported by Meta Ads and Google Ads designed to
            generate attention, enquiries and measurable growth.
          </p>
        </div>
      </article>

      <article class="service-card service-card--web" tabindex="0">
        <div class="service-card__icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <rect x="3" y="4" width="18" height="14" rx="2"></rect>
            <path d="M3 8h18"></path>
            <path d="M7 12h5"></path>
            <path d="M7 15h8"></path>
            <path d="M17 12h1"></path>
          </svg>
        </div>

        <div class="service-card__copy">
          <h3>Website Design &amp; Development</h3>
          <p>
            Modern, fast and conversion-focused websites for Dublin businesses,
            designed to present your brand professionally, explain your services
            clearly and turn visitors into leads and customers.
          </p>
        </div>
      </article>
    </div>
  </div>
</section>

<style>
  .services,
  .services * {
    box-sizing: border-box;
  }

  .services {
    position: relative;
    width: 100%;
    margin: 0;
    padding: 112px 24px 110px;
    overflow: hidden;

    background: #111111;
    color: #ffffff;
  }

  .services__inner {
    position: relative;
    width: min(100%, 1180px);
    margin: 0 auto;
  }

  .services__header {
    width: min(100%, 900px);
    margin: 0 auto 64px;
    text-align: center;
  }

  .services__kicker {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 22px;
    padding: 10px 18px;

    border: 1px solid rgba(180, 220, 255, 0.28);
    border-radius: 999px;
    background: rgba(155, 210, 255, 0.08);
    color: #cfeaff;

    font-size: 12px;
    font-weight: 900;
    line-height: 1;
    letter-spacing: 0.13em;
    text-transform: uppercase;

    box-shadow:
      inset 0 1px 1px rgba(255, 255, 255, 0.18),
      0 0 24px rgba(155, 210, 255, 0.08);
  }

  .services__header h2 {
    margin: 0;
    color: #ffffff;
    font-size: clamp(42px, 6vw, 74px);
    font-weight: 900;
    line-height: 0.95;
    letter-spacing: -0.065em;
    text-wrap: balance;
  }

  .services__header p {
    width: min(100%, 790px);
    margin: 26px auto 0;
    color: rgba(255, 255, 255, 0.68);

    font-size: clamp(17px, 2vw, 20px);
    font-weight: 550;
    line-height: 1.5;
    text-wrap: balance;
  }

  .services__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 26px;
  }

  .service-card {
    --card-accent: 155, 210, 255;

    position: relative;
    isolation: isolate;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-width: 0;
    min-height: 400px;
    padding: 34px;
    overflow: hidden;

    border: 1px solid rgba(var(--card-accent), 0.3);
    border-radius: 26px;
    outline: none;
    background: linear-gradient(
      145deg,
      rgba(23, 29, 34, 0.96),
      rgba(17, 17, 17, 0.9)
    );

    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.045),
      0 28px 70px rgba(0, 0, 0, 0.26);

    transform: translateY(0) scale(1);
    transition:
      transform 360ms cubic-bezier(0.22, 1, 0.36, 1),
      border-color 360ms ease,
      background 360ms ease,
      box-shadow 360ms ease;
    -webkit-tap-highlight-color: transparent;
  }

  .service-card::after {
    content: "";
    position: absolute;
    right: -25%;
    bottom: -35%;
    left: -25%;
    z-index: -1;
    height: 230px;
    pointer-events: none;

    background: radial-gradient(
      circle,
      rgba(var(--card-accent), 0.2),
      transparent 68%
    );
    filter: blur(36px);
    opacity: 0.45;
    transform: translateY(18px) scale(0.96);
    transition:
      opacity 360ms ease,
      transform 360ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .service-card--social {
    --card-accent: 130, 180, 220;
  }

  .service-card--web {
    --card-accent: 190, 225, 255;
  }

  .service-card:hover,
  .service-card:focus {
    border-color: rgba(var(--card-accent), 0.7);
    background: linear-gradient(
      145deg,
      rgba(27, 35, 41, 0.98),
      rgba(18, 21, 24, 0.94)
    );
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.09),
      0 0 42px rgba(var(--card-accent), 0.1),
      0 36px 86px rgba(0, 0, 0, 0.38);
    transform: translateY(-10px) scale(1.012);
  }

  .service-card:hover::after,
  .service-card:focus::after {
    opacity: 0.85;
    transform: translateY(-8px) scale(1.08);
  }

  .service-card:focus-visible {
    outline: 2px solid #cfeaff;
    outline-offset: 5px;
  }

  .service-card__icon {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 68px;
    height: 68px;
    margin-bottom: 54px;

    border: 1px solid rgba(var(--card-accent), 0.16);
    border-radius: 22px;
    background: linear-gradient(
      135deg,
      rgba(var(--card-accent), 0.2),
      rgba(255, 255, 255, 0.055)
    );
    color: #ffffff;

    box-shadow:
      0 0 30px rgba(var(--card-accent), 0.12),
      inset 0 1px 0 rgba(255, 255, 255, 0.12);
  }

  .service-card__icon svg {
    width: 34px;
    height: 34px;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.8;
  }

  .service-card__copy {
    position: relative;
    z-index: 1;
  }

  .service-card h3 {
    margin: 0 0 18px;
    color: #ffffff;
    font-size: clamp(27px, 2.45vw, 34px);
    font-weight: 850;
    line-height: 0.98;
    letter-spacing: -0.055em;
    text-wrap: balance;
  }

  .service-card p {
    margin: 0;
    color: rgba(255, 255, 255, 0.68);
    font-size: 15.5px;
    font-weight: 500;
    line-height: 1.62;
  }

  @media (max-width: 980px) {
    .services__grid {
      grid-template-columns: 1fr;
      width: min(100%, 680px);
      margin: 0 auto;
    }

    .service-card {
      min-height: 340px;
    }

    .service-card__icon {
      margin-bottom: 44px;
    }
  }

  @media (max-width: 640px) {
    .services {
      padding: 92px 16px 82px;
    }

    .services__header {
      margin-bottom: 46px;
    }

    .services__header h2 {
      font-size: clamp(39px, 12vw, 54px);
    }

    .services__header p {
      font-size: 16px;
      line-height: 1.55;
    }

    .service-card {
      min-height: 0;
      padding: 28px;
      border-radius: 24px;
    }

    .service-card__icon {
      width: 62px;
      height: 62px;
      margin-bottom: 48px;
      border-radius: 19px;
    }

    .service-card__icon svg {
      width: 31px;
      height: 31px;
    }

    .service-card h3 {
      font-size: clamp(27px, 8.2vw, 32px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .service-card,
    .service-card::after {
      transition: none;
    }
  }
</style>
