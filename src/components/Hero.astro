<section class="nvs-astronaut-header" aria-labelledby="hero-title">
  <video
    class="nvs-astronaut-video"
    autoplay
    muted
    loop
    playsinline
    preload="metadata"
    aria-hidden="true"
  >
    <source src="/videos/hero-video.mp4" type="video/mp4" />
  </video>

  <div class="nvs-astronaut-overlay" aria-hidden="true"></div>
  <div class="nvs-astronaut-bottom-fade" aria-hidden="true"></div>

  <div class="nvs-astronaut-content">
    <div class="nvs-astronaut-kicker">NextVision Studio</div>

    <h1 id="hero-title">
      <span>Videographer</span>
      <span>Dublin</span>
    </h1>

    <a href="#contact-form" class="nvs-liquid-cta">
      <span>Get a free quote</span>
    </a>
  </div>
</section>

<style>
  .nvs-astronaut-header,
  .nvs-astronaut-header * {
    box-sizing: border-box;
  }

  .nvs-astronaut-header {
    position: relative;
    isolation: isolate;
    width: 100%;
    min-height: 100vh;
    min-height: 100svh;
    margin: 0;
    padding: 130px 24px 160px;
    overflow: hidden;

    display: flex;
    align-items: center;
    justify-content: center;

    background: #111111;
    color: #ffffff;
    text-align: center;
    font-family: Inter, Arial, sans-serif;
  }

  .nvs-astronaut-video {
    position: absolute;
    inset: 0;
    z-index: 0;

    display: block;
    width: 100%;
    max-width: none;
    height: 100%;
    pointer-events: none;

    object-fit: cover;
    object-position: center;
  }

  .nvs-astronaut-overlay {
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;

    background:
      radial-gradient(
        circle at 50% 42%,
        rgba(255, 255, 255, 0.02) 0%,
        rgba(0, 0, 0, 0.24) 44%,
        rgba(0, 0, 0, 0.72) 100%
      ),
      linear-gradient(
        to bottom,
        rgba(0, 0, 0, 0.12) 0%,
        rgba(0, 0, 0, 0.35) 58%,
        rgba(17, 17, 17, 0.96) 100%
      );
  }

  .nvs-astronaut-bottom-fade {
    position: absolute;
    right: 0;
    bottom: -1px;
    left: 0;
    z-index: 2;

    height: 270px;
    pointer-events: none;

    background: linear-gradient(
      to bottom,
      rgba(17, 17, 17, 0) 0%,
      rgba(17, 17, 17, 0.28) 34%,
      rgba(17, 17, 17, 0.76) 70%,
      #111111 100%
    );
  }

  .nvs-astronaut-content {
    position: relative;
    z-index: 3;

    width: 100%;
    max-width: 1120px;
    margin: 0 auto;
  }

  .nvs-astronaut-kicker {
    display: inline-flex;
    align-items: center;
    justify-content: center;

    margin: 0 0 26px;
    padding: 8px 15px;

    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 999px;

    background: rgba(255, 255, 255, 0.08);
    color: rgba(255, 255, 255, 0.82);

    font-size: 10px;
    font-weight: 900;
    letter-spacing: 0.16em;
    text-transform: uppercase;

    backdrop-filter: blur(14px) saturate(145%);
    -webkit-backdrop-filter: blur(14px) saturate(145%);

    box-shadow:
      inset 0 1px 1px rgba(255, 255, 255, 0.2),
      0 14px 34px rgba(0, 0, 0, 0.22);
  }

  .nvs-astronaut-content h1 {
    max-width: 100%;
    margin: 0 auto;

    color: #ffffff;
    font-size: clamp(66px, 9vw, 128px);
    font-weight: 950;
    line-height: 0.88;
    letter-spacing: -0.075em;

    text-shadow: 0 24px 80px rgba(0, 0, 0, 0.68);
  }

  .nvs-astronaut-content h1 span {
    display: block;
  }

  .nvs-liquid-cta {
    position: relative;
    isolation: isolate;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    min-width: 340px;
    min-height: 62px;
    margin-top: 44px;
    padding: 0 52px;
    overflow: hidden;

    border: 1px solid rgba(220, 238, 255, 0.62);
    border-radius: 999px;

    background:
      linear-gradient(
        135deg,
        rgba(210, 232, 248, 0.23),
        rgba(85, 107, 124, 0.20)
      );

    color: #ffffff !important;
    text-decoration: none !important;
    text-transform: uppercase;

    font-size: 16px;
    font-weight: 950;
    letter-spacing: 0.085em;

    backdrop-filter: blur(20px) saturate(170%);
    -webkit-backdrop-filter: blur(20px) saturate(170%);

    box-shadow:
      inset 0 1px 1px rgba(255, 255, 255, 0.42),
      inset 0 -1px 1px rgba(255, 255, 255, 0.1),
      0 0 34px rgba(255, 255, 255, 0.12),
      0 24px 60px rgba(0, 0, 0, 0.42);

    transition:
      transform 0.3s ease,
      background 0.3s ease,
      box-shadow 0.3s ease;
  }

  .nvs-liquid-cta::after {
    content: "";
    position: absolute;
    top: -70%;
    left: -90%;
    z-index: 1;

    width: 72%;
    height: 240%;

    background: linear-gradient(
      120deg,
      transparent,
      rgba(255, 255, 255, 0.55),
      transparent
    );

    opacity: 0.5;
    transform: rotate(18deg);
    transition: left 0.75s ease;
  }

  .nvs-liquid-cta span {
    position: relative;
    z-index: 2;
    text-shadow: 0 1px 14px rgba(0, 0, 0, 0.38);
  }

  .nvs-liquid-cta:hover {
    transform: translateY(-3px) scale(1.035);

    background:
      linear-gradient(
        135deg,
        rgba(220, 240, 255, 0.31),
        rgba(100, 125, 145, 0.26)
      );

    box-shadow:
      inset 0 1px 1px rgba(255, 255, 255, 0.5),
      0 0 44px rgba(255, 255, 255, 0.18),
      0 0 80px rgba(155, 210, 255, 0.12),
      0 28px 64px rgba(0, 0, 0, 0.46);
  }

  .nvs-liquid-cta:hover::after {
    left: 130%;
  }

  .nvs-liquid-cta:active {
    transform: translateY(-1px) scale(0.99);
  }

  .nvs-liquid-cta:focus-visible {
    outline: 2px solid #cfeaff;
    outline-offset: 5px;
  }

  @media (max-width: 1024px) {
    .nvs-astronaut-content h1 {
      font-size: clamp(58px, 10vw, 96px);
    }
  }

  @media (max-width: 767px) {
    .nvs-astronaut-header {
      min-height: 100svh;
      padding: 98px 18px 150px;
    }

    .nvs-astronaut-bottom-fade {
      height: 240px;
    }

    .nvs-astronaut-content {
      width: 100%;
      max-width: 100%;
      padding: 0 4px;
    }

    .nvs-astronaut-kicker {
      margin-bottom: 22px;
      padding: 7px 12px;
      font-size: 9px;
      letter-spacing: 0.14em;
    }

    .nvs-astronaut-content h1 {
      font-size: clamp(42px, 12.4vw, 54px);
      line-height: 0.94;
      letter-spacing: -0.052em;
    }

    .nvs-liquid-cta {
      width: 100%;
      max-width: 340px;
      min-width: 0;
      min-height: 58px;

      margin-top: 34px;
      padding: 0 28px;

      font-size: 13.5px;
      letter-spacing: 0.075em;
    }
  }

  @media (max-width: 380px) {
    .nvs-astronaut-content h1 {
      font-size: clamp(38px, 11.5vw, 48px);
      letter-spacing: -0.045em;
    }

    .nvs-liquid-cta {
      max-width: 310px;
      min-height: 56px;
      font-size: 12.5px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .nvs-liquid-cta,
    .nvs-liquid-cta::after {
      transition: none;
    }
  }
</style>
