<section class="quick-call" aria-labelledby="quick-call-title">
  <a class="quick-call__card" href="#contact-form" aria-describedby="quick-call-description">
    <div class="quick-call__profile">
      <div class="quick-call__image-wrap">
        <img
          src="/images/about/peter-gyarmathy.webp"
          alt="Peter Gyarmathy from NextVision Studio in Dublin"
          width="480"
          height="480"
          loading="lazy"
          decoding="async"
        />
        <span class="quick-call__status" aria-hidden="true"></span>
      </div>
      <div>
        <p class="quick-call__name">Peter Gyarmathy</p>
        <p class="quick-call__role">NextVision Studio</p>
      </div>
    </div>

    <div class="quick-call__content">
      <div class="quick-call__label">Free discovery call</div>
      <h2 id="quick-call-title">Book a Quick Content Call</h2>
      <p id="quick-call-description">
        Tell me about your business, your content goals or the website you need.
        We’ll find the clearest next step together.
      </p>

      <div class="quick-call__actions">
        <span class="quick-call__button">
          Book a call
          <svg viewBox="0 0 20 20" aria-hidden="true">
            <path d="M4 10h11m-4-4 4 4-4 4"></path>
          </svg>
        </span>
        <span class="quick-call__note"><i aria-hidden="true"></i> Free &amp; no pressure</span>
      </div>
    </div>
  </a>
</section>

<style>
  .quick-call,
  .quick-call * {
    box-sizing: border-box;
  }

  .quick-call {
    position: relative;
    width: 100%;
    margin: -1px 0 0;
    padding: 64px 24px 114px;
    overflow: hidden;
    background: #111111;
    color: #ffffff;
  }

  .quick-call::before {
    position: absolute;
    top: 0;
    left: 50%;
    width: 760px;
    height: 300px;
    background: radial-gradient(circle, rgba(155, 210, 255, 0.09), transparent 68%);
    content: "";
    filter: blur(46px);
    pointer-events: none;
    transform: translateX(-50%);
  }

  .quick-call__card {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 185px 1fr;
    align-items: center;
    gap: 42px;
    width: min(100%, 1060px);
    margin: 0 auto;
    padding: 45px 54px;
    overflow: hidden;
    border: 1px solid rgba(190, 220, 242, 0.18);
    border-radius: 32px;
    background:
      linear-gradient(135deg, rgba(255, 255, 255, 0.095), rgba(255, 255, 255, 0.025)),
      rgba(23, 29, 34, 0.88);
    box-shadow:
      inset 0 1px rgba(255, 255, 255, 0.2),
      0 30px 85px rgba(0, 0, 0, 0.38),
      0 0 46px rgba(155, 210, 255, 0.055);
    color: inherit;
    text-decoration: none;
    animation: quick-call-float 6s ease-in-out infinite;
    isolation: isolate;
  }

  .quick-call__card::before {
    position: absolute;
    z-index: -1;
    inset: 0;
    border-radius: inherit;
    background:
      radial-gradient(circle at 13% 50%, rgba(155, 210, 255, 0.1), transparent 27%),
      linear-gradient(110deg, transparent 0 48%, rgba(255, 255, 255, 0.055) 48% 64%, transparent 64%);
    content: "";
    pointer-events: none;
  }

  .quick-call__card::after {
    position: absolute;
    z-index: 3;
    top: -70%;
    left: -55%;
    width: 32%;
    height: 250%;
    background: linear-gradient(110deg, transparent, rgba(215, 239, 255, 0.2), transparent);
    content: "";
    pointer-events: none;
    transform: rotate(16deg);
    animation: quick-call-shine 7s ease-in-out infinite;
  }

  .quick-call__profile {
    position: relative;
    z-index: 2;
    text-align: center;
  }

  .quick-call__image-wrap {
    position: relative;
    width: 126px;
    height: 126px;
    margin: 0 auto 15px;
    border: 1px solid rgba(255, 255, 255, 0.26);
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.08);
    box-shadow:
      0 18px 42px rgba(0, 0, 0, 0.3),
      0 0 30px rgba(155, 210, 255, 0.08);
  }

  .quick-call__image-wrap img {
    display: block;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    object-fit: cover;
  }

  .quick-call__status {
    position: absolute;
    right: 1px;
    bottom: 17px;
    width: 20px;
    height: 20px;
    border: 4px solid #171d22;
    border-radius: 50%;
    background: #31df72;
    box-shadow:
      0 0 0 5px rgba(49, 223, 114, 0.12),
      0 0 20px rgba(49, 223, 114, 0.68);
    animation: quick-call-status 2.4s ease-in-out infinite;
  }

  .quick-call__name,
  .quick-call__role {
    margin: 0;
  }

  .quick-call__name {
    color: #ffffff;
    font-size: 15px;
    font-weight: 850;
  }

  .quick-call__role {
    margin-top: 5px;
    color: rgba(255, 255, 255, 0.48);
    font-size: 12px;
    font-weight: 600;
  }

  .quick-call__content {
    position: relative;
    z-index: 2;
  }

  .quick-call__label {
    margin-bottom: 9px;
    color: #cfeaff;
    font-size: 10px;
    font-weight: 900;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }

  .quick-call__content h2 {
    margin: 0;
    color: #ffffff;
    font-size: clamp(31px, 3.7vw, 45px);
    font-weight: 900;
    letter-spacing: -0.05em;
    line-height: 1.02;
  }

  .quick-call__content p {
    max-width: 680px;
    margin: 15px 0 24px;
    color: rgba(255, 255, 255, 0.64);
    font-size: 15px;
    font-weight: 520;
    line-height: 1.6;
  }

  .quick-call__actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 17px;
  }

  .quick-call__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 9px;
    min-height: 48px;
    padding: 0 24px;
    border: 1px solid rgba(210, 235, 255, 0.31);
    border-radius: 999px;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.16), rgba(155, 210, 255, 0.09));
    box-shadow:
      inset 0 1px rgba(255, 255, 255, 0.3),
      0 14px 34px rgba(0, 0, 0, 0.26);
    color: #ffffff;
    font-size: 14px;
    font-weight: 850;
    transition:
      background 240ms ease,
      box-shadow 240ms ease,
      transform 240ms ease;
  }

  .quick-call__button svg {
    width: 18px;
    height: 18px;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.8;
    transition: transform 240ms ease;
  }

  .quick-call__note {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: #46e780;
    font-size: 13px;
    font-weight: 800;
  }

  .quick-call__note i {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: currentColor;
    box-shadow: 0 0 14px currentColor;
  }

  .quick-call__card:hover {
    border-color: rgba(190, 220, 242, 0.3);
    box-shadow:
      inset 0 1px rgba(255, 255, 255, 0.26),
      0 36px 95px rgba(0, 0, 0, 0.45),
      0 0 55px rgba(155, 210, 255, 0.1);
  }

  .quick-call__card:hover .quick-call__button {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.23), rgba(155, 210, 255, 0.15));
    box-shadow:
      inset 0 1px rgba(255, 255, 255, 0.42),
      0 0 28px rgba(155, 210, 255, 0.13),
      0 18px 38px rgba(0, 0, 0, 0.3);
    transform: translateY(-2px);
  }

  .quick-call__card:hover .quick-call__button svg {
    transform: translateX(3px);
  }

  .quick-call__card:focus-visible {
    outline: 3px solid #cfeaff;
    outline-offset: 6px;
  }

  @keyframes quick-call-float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-8px); }
  }

  @keyframes quick-call-shine {
    0%, 60%, 100% { left: -55%; opacity: 0; }
    68% { opacity: 0.6; }
    84% { left: 125%; opacity: 0; }
  }

  @keyframes quick-call-status {
    0%, 100% { box-shadow: 0 0 0 5px rgba(49, 223, 114, 0.12), 0 0 18px rgba(49, 223, 114, 0.62); }
    50% { box-shadow: 0 0 0 9px rgba(49, 223, 114, 0.035), 0 0 27px rgba(49, 223, 114, 0.9); }
  }

  @media (max-width: 760px) {
    .quick-call {
      padding: 50px 16px 94px;
    }

    .quick-call__card {
      grid-template-columns: 1fr;
      gap: 28px;
      padding: 38px 24px;
      border-radius: 27px;
      text-align: center;
    }

    .quick-call__image-wrap {
      width: 112px;
      height: 112px;
    }

    .quick-call__content p {
      margin-right: auto;
      margin-left: auto;
    }

    .quick-call__actions {
      justify-content: center;
    }
  }

  @media (max-width: 390px) {
    .quick-call__card {
      padding: 34px 19px;
    }

    .quick-call__content h2 {
      font-size: 30px;
    }

    .quick-call__button {
      width: 100%;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .quick-call__card,
    .quick-call__card::after,
    .quick-call__status {
      animation: none;
    }
  }
</style>
